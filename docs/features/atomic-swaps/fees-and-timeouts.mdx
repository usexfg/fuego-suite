---
title: "Fees and timeouts"
description: "swapXFG 1% fee split 69/11/20, timelocks, refunds."
keywords: ["swap fees", "timeout", "refund", "swapXFG", "fuego"]
---

## XFG-side fee

Every completed swap charges **1%** of the XFG amount (`SWAP_FEE_RATE_BPS = 100`).

| Destination | Share |
|-------------|-------|
| CD yield pool | **69%** |
| Bonus Vault | **11%** |
| Treasury | **20%** |

Example: swap **100 XFG** → **1 XFG** protocol fee → **0.69** CD / **0.11** Bonus / **0.20** Treasury (plus normal miner/tx fees).

Not paid to the counterparty. Not the Hearth 1% fee.

## Counterparty fees

ETH gas, BTC/BCH miner fees, etc. are separate.

## Stuck-swap monitoring

| Phase | Typical warning threshold |
|-------|---------------------------|
| Key / setup stall | ~10 minutes |
| Funded but incomplete | ~30 minutes |

Warnings do not auto-refund.

## Refund

After timelock expiry:

```bash
curl -X POST http://127.0.0.1:28080/refund_swap \
  -H 'Content-Type: application/json' \
  -d '{"swap_id": "YOUR_SWAP_ID"}'
```

Refund before expiry fails. Check status with `getswapstatus` / `listswaps`.

## Persistence

Swap state is written after transitions. Restart the daemon and run `listswaps` before acting.
