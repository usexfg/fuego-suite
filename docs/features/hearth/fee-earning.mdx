---
title: "Hearth fee earning"
description: "How the 1% Hearth fee is split between CD yield and LPs."
keywords: ["Hearth fees", "LP", "CD", "1%"]
---

## Protocol trade fee

Every Hearth fill applies:

```
totalFee = notional × 100 / 10000   // HEARTH_FEE_BPS
cdFee    = totalFee / 2
lpFee    = totalFee - cdFee
```

Implemented in `Blockchain.cpp` when processing Hearth/auth pool outputs.

## What LPs should expect

| Item | Reality |
|------|---------|
| Rate | Half of **1%** (effective **0.5%** of notional to LPs in aggregate) |
| Share | Proportional to your LP share of the pool |
| Independence | Separate from **atomic swap** fee routing (69/11/20) |

## What CD holders get from Hearth

The other half of each Hearth trade fee accrues toward **CD yield** plumbing (`cdHearthFeeAccumulator` → CD fee pool path). Holding HEAT CDs participates in that stream together with the CD share of **atomic swap** fees.

## Wrong numbers to ignore

Older docs and strategy notes sometimes say:

- “0.3% to LPs” as the Hearth fee — **incorrect for live `HEARTH_FEE_BPS`**
- “100% of Hearth fees to LPs” — **incorrect** (50/50 with CDs)
- “80% of swap fees to CDs” — **outdated**; atomic swap split is **69/11/20**

Always prefer `src/CryptoNoteConfig.h` and the consensus fee split code.
